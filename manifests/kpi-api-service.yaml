apiVersion: v1
kind: Service
metadata:
  name: kpi-api-service
spec:
  type: NodePort   # accessible depuis l'extérieur via <IP_PUBLIQUE_NOEUD>:30080
  selector:
    app: kpi-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
